MyReads: A Book Lending App
===========================

To install and launch the application:

npm install
npm start

This will install the application and start it automatically.

On launch the application should start in the default browser at:

http://localhost:3006/
